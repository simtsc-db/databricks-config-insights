resources:
  dashboards:
    config_dashboard:
      display_name: "[${bundle.target}] Configuration Insights"
      file_path: ./config_insights.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog}
      dataset_schema: ${var.schema}
      permissions:
        - level: CAN_RUN
          group_name: "users"
