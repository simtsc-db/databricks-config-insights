resources:
  alerts:
    drift_detected:
      display_name: "[${bundle.target}] Config Drift Detected"
      # Reads the settings_drift view (the single source of drift logic: it
      # already classifies value_changed / added / removed and ignores
      # unreadable sentinel values) and keeps only the most recent collection
      # run, so the alert fires only on changes introduced by the latest run.
      # The `changed_settings` column lists exactly what changed for the
      # notification. Always returns exactly one row.
      query_text: |
        SELECT
          COUNT(*) AS drift_count,
          COALESCE(array_join(collect_list(
            concat(setting_name, ' [', COALESCE(workspace_name, '-'), ']: ', change_type,
                   ' (', COALESCE(previous_value, '(none)'), ' -> ',
                   COALESCE(new_value, '(none)'), ')')
          ), '; '), '') AS changed_settings
        FROM ${var.catalog}.${var.schema}.settings_drift
        WHERE detected_at = (
          SELECT MAX(collected_at) FROM ${var.catalog}.${var.schema}.settings_history
        )
      warehouse_id: ${var.warehouse_id}
      evaluation:
        comparison_operator: "GREATER_THAN"
        source:
          name: "drift_count"
          display: "drift_count"
        threshold:
          value:
            double_value: 0
        notification:
          notify_on_ok: false
          subscriptions:
            - user_email: "${workspace.current_user.userName}"
      schedule:
        pause_status: "UNPAUSED"
        quartz_cron_schedule: "0 0 7 * * ?"
        timezone_id: "UTC"
      permissions:
        - level: CAN_RUN
          group_name: "users"

    new_preview_enabled:
      display_name: "[${bundle.target}] New Preview Feature Enabled"
      # Fires when a preview feature flips to enabled vs. its previous
      # observation (or appears already enabled on first sight). A single LAG()
      # window compares each preview setting to its own prior snapshot; the
      # preview-phase filter is aligned with the dashboard views. The
      # `enabled_previews` column rides along for the notification. Always
      # returns exactly one row.
      query_text: |
        SELECT
          COUNT(*) AS new_previews,
          COALESCE(array_join(collect_list(
            concat(setting_name, ' [ws ', CAST(ws AS STRING), ']: ',
                   COALESCE(prev_val, '(new)'), ' -> ', COALESCE(curr_val, '(none)'))
          ), '; '), '') AS enabled_previews
        FROM (
          SELECT setting_name,
                 COALESCE(workspace_id, 0) AS ws,
                 setting_value AS curr_val,
                 LAG(setting_value) OVER (
                   PARTITION BY setting_name, COALESCE(workspace_id, 0)
                   ORDER BY collected_at) AS prev_val
          FROM ${var.catalog}.${var.schema}.settings_history
          WHERE preview_phase IS NOT NULL
            AND preview_phase NOT IN ('GA', 'None', '', 'PreviewPhase.GA')
          QUALIFY ROW_NUMBER() OVER (
                    PARTITION BY setting_name, COALESCE(workspace_id, 0)
                    ORDER BY collected_at DESC) = 1
        ) latest
        WHERE LOWER(curr_val) IN ('true', 'enabled', '1', 'on')
          AND (prev_val IS NULL OR LOWER(prev_val) NOT IN ('true', 'enabled', '1', 'on'))
      warehouse_id: ${var.warehouse_id}
      evaluation:
        comparison_operator: "GREATER_THAN"
        source:
          name: "new_previews"
          display: "new_previews"
        threshold:
          value:
            double_value: 0
        notification:
          notify_on_ok: false
          subscriptions:
            - user_email: "${workspace.current_user.userName}"
      schedule:
        pause_status: "UNPAUSED"
        quartz_cron_schedule: "0 0 7 * * ?"
        timezone_id: "UTC"
      permissions:
        - level: CAN_RUN
          group_name: "users"
